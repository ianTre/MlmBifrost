<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextWhite" parent="TextAppearance.AppCompat">
        <!-- Color que tiene el PlaceHolder del Textbox -->
        <item name="android:textColorHint">@color/editTextColorWhite</item>
        <!-- Dimension del texto -->
        <item name="android:textSize">@dimen/textSizeTextViews</item>
        <!-- Color que tiene el texto del TextBox -->
        <item name="android:textColorPrimary">@color/editTextColorWhite</item>

        <item name="colorAccent">@color/colorPrimary</item>
        <!-- Color cuando el control no está activado -->
        <item name="colorControlNormal">@color/colorPrimary</item>
        <!-- Color que se pone tanto al estar Activado -->
        <item name="colorControlActivated">@color/colorPrimaryDark</item>

    </style>

    <style name="RaisedButtonDark" parent="Theme.AppCompat.Light">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="android:textColor">@color/editTextColorWhite</item>
    </style>

    <style name="Error" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">#FF9800</item>
    </style>
</resources>
